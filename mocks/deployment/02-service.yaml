apiVersion: v1
kind: Service
metadata:
  name: wiremock
  namespace: reboot-patterns
spec:
  type: NodePort
  selector:
    app: wiremock
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
